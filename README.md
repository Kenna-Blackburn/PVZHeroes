
# PVZHeroes for Swift

## Usage

See [/Tests/PVZHeroesTests/Client.swift](/Tests/PVZHeroesTests/Client.swift).

## Contributions

### Style Guide

* Prefer UI names to internal names (ex. prefer Strength to Attack).
* Prefer `struct`s to `enum`s for expandable enumeration (ex. prefer Tribe and Banner as `struct`s).
